#!/bin/sh

git_fetch() {
    find . -type d -name '.git' -print0 | xargs -0 -P "$(nproc)" -I git_dir sh -x -c '
        GIT_TERMINAL_PROMPT=0 git -c credential.helper= --git-dir=git_dir fetch --all </dev/null || true
    '
}

git_fetch "$@"
